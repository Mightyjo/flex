name: Milestone

on:
  release:
    types: [closed]


jobs:
  release:
    name: Close out Milestone
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: info
        run: cat ${{ env.GITHUB_EVENT_PATH }}