language: c

compiler:
  - gcc
  - clang

os:
  - linux
  
jobs:
  include:
  - os: osx
    osx_image: xcode11
    compiler: clang
    env: 
    - PATH=$PATH:/usr/local/Cellar/gettext/0.20.1/bin
#    before_install:
#    - HOMEBREW_NO_AUTO_UPDATE=1 brew install help2man
#    - HOMEBREW_NO_AUTO_UPDATE=1 brew install lzip
#    - HOMEBREW_NO_AUTO_UPDATE=1 brew install texinfo
#    - HOMEBREW_NO_AUTO_UPDATE=1 brew install --fetch-HEAD homebrew/cask/basictex
  exclude:
  - os: linux
    compiler: gcc
  - os: linux
    compiler: clang

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - autoconf
    - automake-1.15
    - autopoint
    - bison
    - gcc-6
    - gettext
    - help2man
    - lzip
    - texinfo
    - texlive
  homebrew:
    update: true
    packages:
    - help2man
    - lzip
    - texinfo
    casks:
    - mactex

script: ./autogen.sh && ./configure && make && make check && make distcheck
